import '@testing-library/jest-dom'
import React from 'react'

// Exponer React globalmente para componentes que usan la transformacion JSX
global.React = React

// any other global test setup can go here
